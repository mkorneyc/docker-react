language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t mkorneyc/docker-react -f Dockerfile.dev .

script:
  - docker run mkorneyc/docker-react npm run test -- --coverage


